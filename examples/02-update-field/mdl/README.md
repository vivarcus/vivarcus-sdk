| 文件 | 说明 |
|------|------|
| [01-object.mdl](01-object.mdl) | 创建 `sdk_demo__c` 与 `title__c` — `vivarcus component apply-mdl --confirm -f mdl/01-object.mdl`，**不要**打进 VPK |

与 [actions/set_title.go](../actions/set_title.go) 的 `Meta()` 保持一致。Recordaction / Objectaction 由 `sdk put` 自动创建且默认 active。
