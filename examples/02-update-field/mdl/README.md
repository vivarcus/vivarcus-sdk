| 文件 | 说明 |
|------|------|
| [01-object.mdl](01-object.mdl) | `RECREATE` `sdk_demo__c`（`title__c`、`stamped__c`）。与 03/05/06/07 相同，不含生命周期。**不要**打进 VPK |

与 [actions/set_title.go](../actions/set_title.go) 的 `Meta()` 保持一致。Recordaction / Objectaction 由 `sdk put` 自动创建且默认 active。
