| 文件 | 说明 |
|------|------|
| [01-object.mdl](01-object.mdl) | `RECREATE` `sdk_demo__c`（`title__c`、`stamped__c`；可重复 `apply-mdl`）— `vivarcus component apply-mdl --confirm -f mdl/01-object.mdl`，**不要**打进 VPK |

与 [actions/set_title.go](../actions/set_title.go) 的 `Meta()` 保持一致。Recordaction / Objectaction 由 `sdk put` 自动创建且默认 active。
